version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app
      - ENVIRONMENT=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  # Uncomment below if you want to run Streamlit alongside
  # streamlit:
  #   build: .
  #   ports:
  #     - "8501:8501"
  #   command: streamlit run streamlit_app/app.py --server.port=8501 --server.address=0.0.0.0
  #   environment:
  #     - PYTHONPATH=/app
  #   volumes:
  #     - ./data:/app/data
